---
name: firestore-rules
description: Implement and refactor firestore rules for better maintainability and readability
---

When you're implementing or refactoring rules, it's important to focus on improving the structure, readability, and maintainability of your rule code. Here are some steps you can take to achieve this:


## Phase 1: Verify Existing Rules
- Make sure all the tests are passing before you start refactoring.
- If not, inform me about the failing tests and we can work together to fix them before refactoring.

## Phase 2: Implement or Refactor Rule Code
-- Feature 1: Data Access Control: USERS and TESTERS
- We also identify two main user roles: `USERS` and `TESTERS`:
  - `TESTERS` are identified by a custom claim in their auth token which is `request.auth.token.tester == true`. `USERS` do not have this claim.
  - The `admin` role is also identified by a custom claim, which is `request.auth.token.admin == true`, and `admins` can access both `/main` and `/test`(if also `TESTERS`) without restrictions. Both `USERS` and `TESTERS` can be admins, but only `TESTERS` can access `/test`.
- On the DB we have two main folders at the root: `/main` and `/test`. The rules for these folders are mostly the same, with some differences in access control which are listed below:
  - For `/main`, only authenticated `USERS` can access their own data, while `admin`s have full access in `/main`.
  - For `/test`, only authenticated `TESTERS` can access the folder; this repo’s test harness defaults most `/test` suite clients to `tester: true`, and only the explicit `USER` vs `TESTER` boundary tests override that default.
- Unauthorized `USERS` should not have access to either folder, and authenticated `USERS` should not have access to the `/test` folder. `admin`s should have full access to both folders regardless of their `USER` or `TESTER` status.
- Implement the rules for both folders, ensuring that the access control logic is correctly applied based on the user roles and authentication status.

-- Feature 2: Data Structure Validation
- At the top level only `/main` and `/test` collections are allowed. No other top-level collections should be permitted.
- Each collection under `/main` and `/test` must follow a specific structure:
  - Each collection must have a 2 documents: One named `config` and one named `data`.
  - The `config` document must contain another folder named `public` that holds a document called `runtime`, which has a boolean field `dataFolder` that indicates the name of the folder in `/{env}/data/{dataFolder}` (with `env` being either `main` or `test`) where the actual data is stored. The `config` also contains another field named `initializedAt` which is a timestamp indicating when the collection was initialized. By default, the `dataFolder` field should be set to "default" and the `initializedAt` field should be set to the server timestamp at the time of collection creation.
  - The `data` document is where the actual data for the collection will be stored, as we said already it will contain a folder named after the `${env}/config/public/runtime.dataFolder` field in the `config` document, and this folder will contain all the user-created documents for that collection.
  - Under `/{env}/data/{dataFolder}/` there will be 2 main folders `public` and `private`. The `public` folder contains documents that are publicly discoverable and possibly readable by other users, while the `private` folder contains a specular set of documents with  private metadata that is only accessible by the owner of the collection and `admin`s. The rules should ensure that the access control for documents in these folders is correctly enforced based on their visibility and ownership. Under the private folder can also find exclusively private data, which has no public counterpart, and is only accessible by the owner and `admin`s. 
  -  Under `/{env}/data/{dataFolder}/public` we will store the following collections:
     - `users`: this collection containes one document for each user that has logged in.
        - Inside `users` collection, each document is named after the user's UID and contains public metadata about the user. Public metadata fields are `name`, `nickname` and `visibility`.
          - `visibility` is a special field that indicates whether the user's profile is publicly visible/discoverable or not. If `visibility` is set to `public: true`, then other users can read the user's metadata.
          - `nickname` is a special field that is required if `visibility.public` is set to true, and it must be unique across all users. The rules should enforce that if a user sets their profile to public, they must provide a unique nickname, and that nickname cannot be used by any other user with a public profile.
          There another public collection named `nickiname-index` that contains a document for each nickname that is currently in use, with the nickname as the document ID and a reference to the corresponding user document as value. This collection is used to enforce the uniqueness of nicknames among publicly visible profiles.

           - `items`: this collection contains all the user-created documents for that collection, and these documents are publicly discoverable and possibly readable by other users based on the collection's visibility settings. The rules should ensure that the access control for these documents is correctly enforced based on their visibility and ownership.
              - Each document in the `items` collection is named after the item's UUID and contains the following fields:
                - `name`: the name of the item, which is a required string field that must be between 3 and 100 characters long. The rules should enforce that this field is present and meets the length requirements when a new item document is created.
                - `description`: an optional string field that provides a description of the item, with a maximum length of 1000 characters. The rules should enforce that if this field is provided, it does not exceed the maximum length.
                - `userId`: a required string field that contains the UID of the user who owns the item. The rules should enforce that this field is present and matches the UID of the authenticated user when a new item document is created, and that it cannot be modified after creation. This field is used to determine the ownership of the item and to enforce access control based on ownership.
                - `createdAt`: a required timestamp field that indicates when the item was created. The rules should enforce that this field is set to the server timestamp at the time of item creation, and that it cannot be modified after creation. This field is used for record-keeping and can also be used for sorting and querying items based on their creation time.
                - `updatedAt`: a required timestamp field that indicates when the item was last updated. The rules should enforce that this field is automatically updated to the server timestamp whenever the item document is modified, and that it cannot be manually set or modified by users. This field is used for record-keeping and can also be used for sorting and querying items based on their last update time.
                - `visibility`: a required object field that indicates the visibility settings of the item. It contains a boolean subfield: `public`. The rules should enforce that `visibility.public` is present and is a boolean value when a new item document is created, and that it can only be modified by the owner of the item or `admin`s. When `visibility.public` is set to true, the item is publicly readable and discoverable by other users, while when it is set to false, only the owner and `admin`s can read the item document. Otherwise only the owner and `admin`s can read the item document.
     - `nickname-index`: this collection contains a document for each nickname that is currently in use, with the nickname as the document ID and a reference to the corresponding user document as value. This collection is used to enforce the uniqueness of nicknames among publicly visible profiles. 
        - The nickname document has the following fields:
          - `userId`: a required string field that contains the UID of the user who owns the nickname. The rules should enforce that this field is present and matches the UID of an existing user document in the `users` collection when a new nickname document is created, and that it cannot be modified by other users, only the onwer can. This field is used to determine which user owns the nickname and to enforce access control based on ownership.
          - `createdAt`: a required timestamp field that indicates when the nickname was created. The rules should enforce that this field is set to the server timestamp at the time of nickname creation, and that it cannot be modified after creation. This field is used for record-keeping and can also be used for sorting and querying nicknames based on their creation time.
          - `updatedAt`: a required timestamp field that indicates when the nickname was last updated. The rules should enforce that this field is automatically updated to the server timestamp whenever the nickname document is modified, and that it cannot be manually set or modified by users. This field is used for record-keeping and can also be used for sorting and querying nicknames based on their last update time.  

      
    
  - Under `/{env}/data/{dataFolder}/private` we will store the following collections:
     - `authorized-users`: this collection contains one document for each user that is authorized to login to the app. Inside this collection there is a document that has the user's email as key and the field `addedAt` as value, which is a timestamp indicating when the user was authorized. The rules should ensure that only `admin`s can write to this collection, while the onwner of that email can read it (which is the user with that email).
     - `users`: this collection is a specular copy of the public `users` collection, but it contains private metadata about the user that should not be publicly visible (e.g., email, auth provider, etc.). The rules should ensure that only the owner of the user document and `admin`s can read and write to this collection, while it is not accessible by other users. The private metadata fields are:
        - `email` is a special field that contains the user's email address, and it is required for each user document in the private `users` collection. The rules should enforce that when a new user document is created in the private `users` collection, it must contain a valid email address, and that email address must be unique across all user documents in the private `users` collection. This means that no two user documents in the private `users` collection can have the same email address, and that email address cannot be used by any other user document in the private `users` collection.
        - `lastLogin` is a special field that contains the timestamp of the user's last login, and it is automatically updated to the server timestamp whenever the user logs in. The rules should enforce that this field is only writable by the owner of the user document and `admin`s, and that it is not modifiable by other users.
     - For the moment under `{env}/data/{dataFolder}/private` we only store the aforementioned `authorized-users` collections.
- The `visibility.public` field structure is used the same way for `users` and `items` documents, but the access control logic based on this field is applied differently for each type of document. For `users` documents, when `visibility.public` is set to true, the user's profile is publicly visible and discoverable by other users, while when it is set to false, only the owner and `admin`s can read the user's profile. For `items` documents, when `visibility.public` is set to true, the item is publicly readable and discoverable by other users, while when it is set to false, only the owner and `admin`s can read the item document. The rules should ensure that the access control logic based on the `visibility.public` field is correctly enforced for each type of document according to its specific requirements.
